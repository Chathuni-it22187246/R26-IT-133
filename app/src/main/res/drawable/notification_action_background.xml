<?xml version="1.0" encoding="utf-8"?>
<!--
  Supplied so resource linking succeeds when androidx.core's
  notification_action_background fails to merge from the local Gradle cache.
  Matches the style reference in core values-v21 Widget.Compat.NotificationActionContainer.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#40FFFFFF">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFFFF" />
            <corners android:radius="2dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#00000000" />
            <corners android:radius="2dp" />
        </shape>
    </item>
</ripple>
